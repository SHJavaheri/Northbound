# Ambient Audio Assets

Drop your preferred ambient track into this folder and name it `ambient.mp3`. The site assumes that file exists at `/audio/ambient.mp3` and will try to autoplay it when a visitor opens the page. If a browser blocks autoplay, the music will start as soon as the user interacts with the window (e.g., clicks or presses a key).
